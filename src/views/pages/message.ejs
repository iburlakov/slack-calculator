<!DOCTYPE html>
<html>
<head>
    <title><%=title %></title>
    <meta charset="utf-8" />
</head>
<body>
    <h1><%=title %> as <%=email %></h1>
    <form action="/message" method="POST">
        <label for="message">Message</label>
        <input type="text" name="textMessage" />
        <select name="chanId">
              

                    <% for ( var i = 0; i < convs.length; i++ )
                    {
                        %>
                        <option value="<%=convs[ i ].id %>"><%=convs[i].name %></option>
                    <%
                    }
                    %>
        </select>
        <input type="submit" value="Send"/>
    </form>
    <span><%= status%></span>
</body>
<html>